# Default Prompt Assets

This directory contains the distributable default prompt used by the release
layer demos.

Files:

- `prompt.wav`: default prompt audio
- `prompt.txt`: matching prompt transcript
- `prompt_track.json`: prompt-side duration track extracted with MFA
- `prompt_alignment_raw.json`: raw MFA alignment output for the prompt
- `prompt_alignment_debug.json`: alignment runtime metadata
